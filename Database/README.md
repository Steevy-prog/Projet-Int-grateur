# Projet-Int-grateur

⚠️ One important note: the password hashes in `07_sample_data.sql` are placeholders. Before running, replace them with real bcrypt hashes generated by your backend (using `bcrypt.hash()` in Node.js or equivalent).
